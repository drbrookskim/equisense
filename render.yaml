services:
  - type: web
    name: todaysstockanalyzer
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn stock_analyzer.api:app --host 0.0.0.0 --port $PORT --loop asyncio --http h11
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0
